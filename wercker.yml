box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        flags: --buildDrafts=true
deploy:
  box: samueldebruyn/debian-git
  steps:
    - ysqi/git-push:
        gh_oauth: $GIT_TOKEN
        repo: c1ay/c1ay.github.io
        gh_pages: true
        branch: master
        basedir: public
        tag: test
